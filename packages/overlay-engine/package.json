{
  "name": "@botexe/overlay-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Overlay-DSL (Schema + Validierung) und schlanker Runtime-Renderer für den TTLS-Browser",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "echo '(overlay-engine: tests folgen in schritt 3)'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^5.9.3"
  }
}
